# Branch note

This repository was created with `main11` as its default branch. All current verified DirectorCut 0.2 source is on `main11`.

Until the GitHub repository default branch is renamed in repository settings, use `--branch main11` when you want to be explicit while cloning.
