# DirectorCut repository status

Current runnable milestone: **0.2 local-first MVP**.

Verified on GitHub Actions:
- C++20 core configures and builds on Ubuntu.
- Native core tests pass.
- Desktop JavaScript syntax checks pass.
- Media utility tests pass.
- Local transcription adapter compiles as Python.

The immediately runnable desktop shell is Electron + local FFmpeg. Qt/QML is included as the path toward the native professional UI. GStreamer Editing Services, GPU playback, advanced NLE trim modes, generated motion graphics, recording Director, and multimodal Director automation are roadmap work rather than completed 0.2 features.
