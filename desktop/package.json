{
  "name": "directorcut-desktop",
  "version": "0.2.0",
  "private": true,
  "description": "Local-first DirectorCut desktop shell",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check media-utils.js && node --check ../prototype/app.js",
    "test": "node tests/media-utils.test.js"
  },
  "devDependencies": {
    "electron": "44.2.0"
  }
}
